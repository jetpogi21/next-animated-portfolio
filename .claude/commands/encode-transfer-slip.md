# Encode Transfer Slip

**Not used in this portfolio repository.** The steps below were for a manufacturing / sales admin system elsewhere.

If you genuinely need this workflow (parse a transfer slip image and write to a database), reintroduce it with **this** app’s schema and APIs — do not assume tables like **`vibram_sales.production_items`** exist here.

---

## Archived stub

The original long-form command described OCR-style extraction and SQL inserts for another product. It has been removed from this repo to avoid misleading agents. Keep domain-specific playbooks next to the code that implements them (e.g. **`docs/`**).
