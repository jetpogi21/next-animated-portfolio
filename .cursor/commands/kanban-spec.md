# Kanban / Panda spec

**Not applicable** to this portfolio repository. The **`/kanban-spec`** flow referenced **`apps/panda-realty/KANBAN-SPEC.md`** in another monorepo.

If you add a product spec for **this** site, store it under **`docs/`** (create the folder if needed) and link it from **`CLAUDE.md`**.
